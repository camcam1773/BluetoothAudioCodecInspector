name: .NET Core Desktop

on:
  push:
    branches: [ "!master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-2022
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - run: |
          dotnet build -c Release
